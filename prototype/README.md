# Parked volume experiment

These unintegrated files were authored while the rendering-direction question was pending. The user selected a constrained photographic rig instead. This checkpoint preserves the experiment honestly; it is not an accepted renderer or active console feature. Current photographic work lives in `src/render/photographic.ts` and `src/study/photographic.ts`.
