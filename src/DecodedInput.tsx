import {useLayoutEffect,useRef} from 'react';
export function DecodedInput({image}:{image:HTMLImageElement|null}){const ref=useRef<HTMLCanvasElement>(null);useLayoutEffect(()=>{if(!ref.current)return;const c=ref.current;if(!image){c.getContext('2d')!.clearRect(0,0,c.width,c.height);return;}c.width=image.naturalWidth;c.height=image.naturalHeight;c.getContext('2d')!.drawImage(image,0,0);},[image]);return <canvas ref={ref} data-input-ready={!!image} aria-label="Exact decoded PNG supplied to the neural encoder"/>;}
