Specimen Archive now runs a continuous intact neural/browser exhibit with synchronized apparatus, specimen, input/neural and controlled-browser views.

The executable path is captured PNG → published 47-neuron/161-edge circuit → six computed motor outputs → fixed phase decoder → genuine Playwright cursor, wheel and click events → next PNG. All mappings and assumptions are documented; this is a controlled colour-assisted task, not general web intelligence.

Measured evidence from clean source f2e3f2a:

- Standard held-out layout: link followed and button activated.
- Offset held-out layout: link followed, then fixed budget exhausted before activation.
- Low contrast: target detection failed after two scrolls.
- Both matched neural interventions: zero controller actions in all six cases.
- Nine exact replays: 432 decisions and 25,920 all-neuron samples. Independent Linux CI repeated the outcomes.
- Two real observers for 600.966 seconds: 11,066 matching packets, four completed episodes, no page errors or dropped stream frames, exact disconnected specimen freeze and successful reconnect.
- Actual 46.4-second integrated preview; verified recorder commits and no duplicate after receipt loss.

Install with `npm ci`, `npx playwright install chromium`, then `npm run dev`. Windows defaults to installed Edge; set `PLAYWRIGHT_CHANNEL=chromium` to use the downloaded browser. No credentials are required for local operation. Publication is optional and uses normal GitHub CLI authentication.

Download `specimen-exhibit-evidence.zip` and extract at the repository root to restore the selected raw evidence under ignored `runtime/`. Run `npm run exhibit:replay -- runtime/exhibit-validation/exhibit_1789141958520_5256737e`. The dashboard then opens the raw traces and videos for the compact archived records. The integrated preview is at `/docs/previews/integrated-review.html`.

The release ZIP is outside ordinary Git history. Its SHA256 is `2b5f07ddb786de4f969a3eb6c80ecd2b4d8b4c6b5fcda207409d79ab7ffcc72f` (44,372,030 bytes). It contains original controlled-page recordings and generated exhibit imagery, no third-party browser session or credentials. Data/reference/generated-asset/font licences and provenance are in `THIRD_PARTY_NOTICES.md`.

See `docs/EXHIBIT_CONTROLLER.md`, `docs/EXHIBIT_VALIDATION.md`, `docs/WORKSHEET.html` and `STATUS.md` for definitions, reproduction commands and limitations. The original frozen lane controller, scientific checks, photographic head/root fixes and development history are preserved.
