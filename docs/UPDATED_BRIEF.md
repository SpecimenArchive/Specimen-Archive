IMPLEMENT THIS BRIEF NOW IN THE EXISTING PROJECT

Treat this message as an instruction to implement the complete milestone below. Begin working in the repository immediately. Reading the brief, auditing the build and making a plan are the first steps of the implementation.

After a brief status update, continue directly to code and asset changes, running the application, inspecting the result and fixing concrete problems. A summary of the requirements or a list of missing features does not complete this task. Do not end the turn after explaining what needs to be built while useful, authorised implementation work remains.

You have direction to make the routine edits, generate the required assets, run the existing tools and perform the verification described below. Use the existing project and preserve its working components. Apply existing repository instructions and permissions. If a required capability is unavailable, identify the exact limitation and continue the parts that are unblocked; never claim unperformed work is complete.

I am available for focused creative guidance at concrete previews. Ask when my answer would materially change the result, and continue independent work while awaiting it. You do not need to ask whether to start or whether to proceed from the audit to implementation.

Track the requirements through completion. Finish with the running preview, what actually changed, the checks performed and any remaining blockers or limitations. Base the completion report on the implemented and observed result.

FULL MILESTONE BRIEF — PRESERVE EVERY REQUIREMENT BELOW

Continue the existing Specimen Archive project and complete it as a coherent laboratory exhibit with four connected observation views, a functioning neural browser controller, and reproducible public source code.

This is the consolidated instruction for the current milestone. It replaces the earlier overlapping implementation and visual prompts. Work in the existing repository, preserve completed functionality, and carry the work through to an inspectable result.

1. Exhibition context and core objective

The umbrella is Specimen Archive. The current exhibit is Specimen 01, ticker $LARVA, based on Platynereis dumerilii and the selected larval reference stage.

The intended audience receives a companion worksheet before viewing the exhibit. That worksheet identifies the fictional laboratory setting, generated apparatus and specimen imagery, computational neural model, and genuinely executed software actions. The observation interface can remain in character without repeated simulation banners. Keep that worksheet and technical provenance accurate and available with the exhibit. Laboratory possession and housing language belongs to the staged narrative.

Our technical objective is verifiable working software. A developer should be able to run the controller, observe actual browser actions, trace those actions through the neural model, and reproduce meaningful intervention checks. Maintain accurate attribution in the source, experiment reports and Methods. A rendered bench is exhibition scenery; it is not experimental evidence.

The public experience is observation-only. Visitors can inspect neurons, switch views and explore recordings; they cannot change stimuli, drive the cursor or alter the active experiment.

2. Audit and preserve the existing work

Read the project instructions, relevant skills, code, assets, research notes, tests and current Git history. Run the app and inspect it in a browser before planning replacements.

Trace the existing data-to-model-to-browser pathway and identify its actual entry points. Verify the active circuit size; previous screenshots reported 47 cells and 161 directed connections. Preserve working neural dynamics, browser controls, microscopy assets, recordings and GitHub integration.

Previous screenshots showed intact runs marked activated and motor-clamped or photoreceptor-disconnected runs marked not activated. Verify those records. Zero motor activity in a disconnected control is not automatically a bug.

Briefly report what already works and the concrete gaps, then proceed. Do not stop after an audit or restart the project unnecessarily. Keep existing repository authentication scoped correctly and preserve unrelated account settings.

3. Visual direction and apparatus

Create a photoreal laboratory workstation with a plausible microscope, transparent specimen chamber, illumination equipment, camera attachment and connected computer. Use microscopy references to ground the arrangement.

Use practical laboratory lighting, restrained colours, believable optics, materials and reflections, and naturally routed equipment cables. Maintain the graphite console, pale microscopy field and restrained cyan or amber accents. Keep labels readable.

Cables should connect the camera, illumination equipment and workstation. Avoid invented wires embedded in the larva, decorative electrodes, fabricated staff credentials or affiliations.

Establish one master apparatus composition. The bench overview, stage close-up and magnified observation must belong to the same setup. Maintain equipment placement, chamber geometry, lighting and specimen orientation across views. Where a monitor is visible, composite the actual browser or telemetry into its screen with the correct perspective.

Use available image-generation, rendering, frontend and browser-inspection skills where useful. If a focused reusable project skill would materially improve this work, create one according to the applicable skill-creation workflow. Choose an implementation that achieves the visual result without unnecessary infrastructure.

Show one apparatus still at its intended display size early. Establish a convincing foundation before investing in extended animation.

4. Implement all four connected observation views

A. Apparatus overview
Show the complete microscope workstation and its relationship to the specimen chamber and computer. Provide a closer stage view if it adds useful context. Keep this expandable so it does not displace the active technical demonstration.

B. Specimen observation
Show the magnified specimen using the strongest existing photographic microscopy treatment. Maintain recognisable reference anatomy, translucent depth, restrained pigment, selective focus and coherent optical boundaries.

The specimen needs local deformation: subtle bending and supported movement of fine appendages. Keep internal structures anchored to the body. Avoid whole-image rotation as the main animation, repeated rigid compartments, rubbery stretching, texture sliding and uniform artificial grain.

Constrain orientation to what the renderer can convincingly support. Keep active behaviour connected to the model outputs. Document decorative optical effects separately from computed behaviour. A stationary bench view is acceptable; prioritise the specimen and controller.

C. Signal inspection
Give signal inspection its own visible panel. Show the actual browser image or sampled region supplied to the encoder, its encoded sensory inputs, selected neuronal activity, motor outputs and decoder thresholds.

Provide a compact activity history across the selected circuit and a detailed trace for an inspected neuron. Display source IDs, actual connections and units. Use measured anatomical locations only when the data supports them; otherwise identify the layout as schematic in the technical explanation.

Record quantities the engine actually computes. A rate-based model must not acquire invented voltage, spike or calcium measurements merely to look more technical.

D. Browser execution
Show the actual controlled browser, its cursor, page changes and executed actions. Display the current task or page and a compact event feed. Keep the controlled browser independent of the observer's own browser.

At desktop size, make specimen observation, signal inspection and browser execution visible together. Integrate the apparatus overview as an expandable context view. On narrower screens, keep all four accessible with clear shared session context.

Avoid unnecessary blank space, tiny text and long repeated evidence cards. Keep the experience visually coherent as one instrument.

5. Preserve a genuine sensory-to-action feedback loop

Load the actual selected connectome data with traceable neuron IDs and connections. Document its source, filtering, coverage and licences. Distinguish published anatomy from assumed dynamics and control mappings.

Use the existing neural engine wherever appropriate. Browser pixels must become documented sensory-neuron inputs; the neural computation must produce the outputs used by the action decoder.

Implement or verify an explicit decoder for movement and activation. Explain its population selections, thresholds, timing and engineering choices. Execute the decoded commands through Playwright or the existing browser adapter, then feed the resulting browser image back to the sensory encoder.

For the initial visual tasks, keep DOM target coordinates, task answers and success checking outside the controller's decision pathway. DOM inspection may support evaluation or reject prohibited actions, but must not silently select the successful action for the neural controller.

Keep setup, environment resets and recovery assistance separately identified. Do not introduce hidden target snapping, scripted completion or an LLM choosing actions behind the neural display.

Use isolated browser sessions for observation tasks. My manual token deployment remains separate from controller-generated actions; any contract or receipt display must use the actual transaction and accurate attribution.

6. Synchronise the four views and make events inspectable

Use one authoritative backend session. Track session and run IDs, source revision, configuration, input frame IDs, model steps, command IDs, execution outcomes and timestamps.

The signal display, browser commands and model-dependent specimen motion must derive from the same engine state. Apparatus screens and indicators should reflect that session where appropriate.

Make an action inspectable: selecting an event reveals the input frame, encoded stimulus, relevant neural outputs, decoder calculation, executed command and observed result. Historical inspection should use aligned recorded data without altering the active experiment.

Distinguish model time from elapsed wall time, image capture time and action execution time. Decouple display frame rate from model integration cadence. Do not fabricate extra neural samples or shorten required integration simply to make the display look faster.

7. Continuous operation and streaming

Default the homepage to the current intact controller session. Completed experiments and disabled-circuit controls belong in an archive with clearly identified conditions. Selecting an archived run must not replace the public backend's active state.

Continue through bounded episodes or tasks, with recorded episode transitions and state resets. Preserve or reset neural state according to the documented model; do not imply continuity that the implementation does not maintain.

Handle browser failures, page timeouts, stalled episodes and observer reconnects. Record recovery assistance and expose actual live, replay, complete, idle and disconnected states. Do not replay old footage under a live software status.

Visitors must subscribe to the same server session. A page refresh must not create a separate experiment for each visitor. Use bounded telemetry histories and an efficient frame stream; avoid continually uploading frames to Git.

Measure actual runtime performance and use sensible resource limits. Preserve the project's current stack unless a concrete limitation requires change.

8. Extend behaviour and validate it

Preserve the existing benchmark and meaningful controls. Freeze a baseline configuration, then test a defined set of previously unused target positions, layouts and contrast conditions. Record failures as well as successes.

Progress from the current target task to movement, scrolling and following links across a small set of controlled pages. Document any input highlighting or environment assistance. Only extend to selected public pages after those behaviours are demonstrated reliably enough to evaluate.

Compare intact operation with motor outputs disabled and a relevant sensory-to-motor pathway interrupted. State the expected effects before interpreting the results. If the causal path remains ambiguous, add a targeted control rather than broadly multiplying tests.

Include replayable traces and a real browser recording. Replay establishes reproducibility of the recorded computation; intervention results provide evidence for the implemented pathway. Neither alone establishes biological fidelity or general browser competence.

After integrating the new runtime and views, verify at least a ten-minute continuous session, matching session IDs and events across two clients, and correct reconnection behaviour. Reuse existing validation when unchanged components are already sufficiently checked.

9. Public GitHub evidence and documentation

The public repository must contain the actual runnable neural engine, data preparation, sensory encoding, motor decoder, browser adapter, telemetry recording and replay code.

Keep genuine development commits with actual authorship and timestamps. Preserve existing history. Publish tested increments and compact experiment records at meaningful boundaries; prevent duplicates and keep continuous raw telemetry and large recordings outside ordinary Git commits.

Keep the recorder automation clearly attributed. Show actual publication success, pending state or failure, and link real commits from the evidence archive.

Provide an accurate README, architecture explanation, equations or update rules, data provenance, assumptions, licences, setup commands, example run, reproduction commands and current limitations. Check tracked files for credentials before publication.

Replace the repeated experiment cards with a compact comparison table and expandable traces, browser recordings and source links.

Maintain the companion worksheet as a clear distinction between generated exhibition imagery, computed model signals, published anatomical data and executed browser actions. Keep scientific claims precise.

10. Research references and scope

Use these references as appropriate and verify details needed by the implementation:

- Whole-body larval connectome: https://elifesciences.org/articles/97964
- Published atlas: https://jekelylab.github.io/Platynereis_connectome/
- Visual sensory-motor circuit: https://elifesciences.org/articles/02730
- Ciliomotor circuitry and imaging references: https://elifesciences.org/articles/26000
- Comparative fly implementation: https://github.com/fruitflydev/flycoinrh
- Playwright actions: https://playwright.dev/docs/input
- Playwright traces: https://playwright.dev/docs/trace-viewer

Use the fly project as an architectural reference. Keep this implementation original and species-appropriate. Do not copy fly-specific sensory anatomy or learning circuitry into the larval model without justification.

Finish the four-view exhibit and continuous controller before enlarging the circuit, adding learning, or introducing an LLM journal. Record those as later possibilities with prerequisites. Do not present deferred features as implemented.

11. Working process and completion

I am available for guidance. Ask focused questions when a creative decision, missing asset or consequential choice would materially change the result. Make routine implementation decisions independently and continue unblocked work while awaiting an answer.

Use concrete review checkpoints: apparatus still, short integrated motion study, then functioning continuous exhibit. Maintain a concise STATUS.md with completed work, active limitations and next steps. Avoid prolonged invisible work or repeated cosmetic passes that do not address the actual weakness.

Finish with:
- All four connected views integrated into one coherent interface.
- A working sensory-to-neural-to-browser feedback loop with traceable actions.
- A continuous shared session with accurate status and recovery.
- Reproducible experiments and compact public evidence.
- A polished apparatus and specimen presentation with accurate companion documentation.
- Runnable source, setup instructions, actual validation results and an honest account of remaining limitations.

The work is complete when a briefed visitor can understand the laboratory scene and watch the software act, and a developer can independently reproduce and inspect the claimed technical behaviour.

START NOW: inspect the existing repository and running application, give a concise implementation update, and then begin the first missing integrated slice. Continue through the milestone and its review checkpoints. Reading this document is the starting action; the deliverable is the implemented, verified exhibit described above.
