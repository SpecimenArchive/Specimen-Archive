# Integrated observation review

The user approved the fixed apparatus direction and authorized self-assessment at subsequent checkpoints. The accepted `photo-rig-v3-root-stable` renderer and its tissue asset were preserved.

The production preview captures 46.4 seconds at the actual 1440 × 1050 observation-window size, starting with the expanded apparatus and continuing to the three simultaneous primary views, a recorded action inspection and return to live. Video is at 25 fps; browser display cadence is measured separately by the endurance check. No time compression or generated browser footage is used.

Observed and corrected: the initial apparatus composite showed thin triangular seams on the green target. The stage detail is a crop from the same master; equipment never changes geometry or moves. Screen brightness remains restrained, with one small actual model-step/motor readout. The actual input image and current controlled-browser image are distinguished.

The specimen retains local postural bending, motor-dependent regional tissue deformation and independently phased distal appendages. Head tissue and six chaetal roots retain the previously accepted masks. The background does not deform. Optical texture is fixed to registered anatomy, with bounded local displacement; this is still a synthetic layered specimen at a constrained angle, not a volumetric reconstruction or measured live-animal recording. Repeated views during recurrent inputs are expected from the deterministic engine; no looping video supplies the specimen motion.

A selected event reveals its input and after-image hashes, model window, all neuron samples, motor thresholds, command and trusted executed events. Replay changes only that observer's display. All three primary panels and the apparatus use the selected run/model state. A disconnected observer freezes; reconnect returns to the existing backend session.

The 47-cell rate model and the simple assisted task remain the scientific/technical boundary. No general browser competence, learning, unrestricted specimen rolling or physical experimental apparatus is claimed. Failure on reduced contrast and the offset-layout action budget are retained in the evidence table.
