# External control audit — 12 September 2026

**Verified for neural wheel proposals and acknowledged scrolling. Coin selection, navigation and initial pointer placement are supervisor orchestration. Neural clicking, coin selection and general page understanding are not implemented in the external profile.** This verdict refers to the running Windows implementation, not the preserved coloured-target benchmark.

The implementation checkpoint is [a80d5082090ede7a6b4437058d3da5dbe4090f46](https://github.com/SpecimenArchive/Specimen-Archive/tree/a80d5082090ede7a6b4437058d3da5dbe4090f46), deployed clean on the VM. It includes the same controller as a0f8396; a80d508 adds the predeclared evaluation harness. Later result/documentation commits identify their executed source separately. Earlier Windows checkpoint 7f72d2b browsed its own observation website; the active external profile supersedes that schedule.

## Executed chain and ownership

| Capability | Executing source at the audited commit | Decision owner and verification |
|---|---|---|
| Native pixels | [remote-desktop.ts](https://github.com/SpecimenArchive/Specimen-Archive/blob/a80d5082090ede7a6b4437058d3da5dbe4090f46/server/exhibit/remote-desktop.ts), `nativeView`; [NativeDesktop.cs](https://github.com/SpecimenArchive/Specimen-Archive/blob/a80d5082090ede7a6b4437058d3da5dbe4090f46/scripts/windows/NativeDesktop.cs) | Guarded Windows GDI exposure. The 1280 × 665 sensory crop is taken from that same 1280 × 800 desktop exposure, starting at (0,87). Original PNGs and hashes are retained; replay verifies the crop pixel for pixel. |
| Sensory encoding | [observation-encoder.ts](https://github.com/SpecimenArchive/Specimen-Archive/blob/a80d5082090ede7a6b4437058d3da5dbe4090f46/server/exhibit/observation-encoder.ts), `encodeObservation` | PNG bytes only. Upper/lower luminance contrast is engineered perception. No URL, DOM target, scroll offset, seed or outcome enters the encoder. |
| Neural computation | [controller.ts](https://github.com/SpecimenArchive/Specimen-Archive/blob/a80d5082090ede7a6b4437058d3da5dbe4090f46/server/exhibit/controller.ts), `observe`; [engine.ts](https://github.com/SpecimenArchive/Specimen-Archive/blob/a80d5082090ede7a6b4437058d3da5dbe4090f46/server/model/engine.ts) | 600 synchronous rate-model steps per capture, 47 actual cell activities, 60 retained full-state samples. The engine checkpoint persists across recording segments. |
| Wheel direction | [controller.ts](https://github.com/SpecimenArchive/Specimen-Archive/blob/a80d5082090ede7a6b4437058d3da5dbe4090f46/server/exhibit/controller.ts), `decodeExhibit` | Decoder receives computed motors and the fixed scroll phase. M < 0.500 waits; otherwise D ≥ 0.008 selects +48 px, else −48 px. D is MN3_r #1732111 minus MN2_r #359142. Direction is not a scripted constant gated by activity. |
| Acceptance and dispatch | [external-station.ts](https://github.com/SpecimenArchive/Specimen-Archive/blob/a80d5082090ede7a6b4437058d3da5dbe4090f46/server/exhibit/external-station.ts), `episode` | Guards block stale input, unapproved routes, form controls and hidden/invalid capture. The accepted command executes through `page.mouse.wheel`. Records separate proposal, acceptance, dispatch, trusted acknowledgement and observed displacement. |
| Result check | Same module, `position` and receipt construction | DOM reads identify the scroll container beneath the fixed cursor and measure before/after displacement. These reads never select direction or a target. Acknowledged boundaries, policy blocks, waits and failures remain distinct from movement. A missing trusted event causes recovery. |
| Pointer placement | Same module, `navigate` | Explicit supervisor placement at (512,465) CSS px. This is not neural pointing. |
| Page navigation | [external-policy.ts](https://github.com/SpecimenArchive/Specimen-Archive/blob/a80d5082090ede7a6b4437058d3da5dbe4090f46/server/exhibit/external-policy.ts), `EncounterPlanner`; `navigate` above | Supervisor discovers approved coin links, chooses least-visited destinations, returns to Explore, and periodically opens approved research. No random neural destination, fallback click or target snapping is claimed. |
| Memory and retries | [store.ts](https://github.com/SpecimenArchive/Specimen-Archive/blob/a80d5082090ede7a6b4437058d3da5dbe4090f46/server/memory/store.ts), [adapter.ts](https://github.com/SpecimenArchive/Specimen-Archive/blob/a80d5082090ede7a6b4437058d3da5dbe4090f46/server/memory/adapter.ts) | Persistent experience software retrieves evidence; an explicitly identified bounded supervisor adapter can change retry budget 1–3. Neural weights, sensory drives and decoder thresholds are unchanged. |

The active construction path is `start-backend.ps1` → `EXHIBIT_EXTERNAL_PROFILE=1` → `ExhibitService.external` → `ExternalStation.episode`. The service chooses this runner before the legacy `runEpisode` branch. The old `observation-runner.ts`, `station-tabs.ts`, fixture `runner.ts` and `server/browser/{runner,executor}.ts` retain previous mouse/navigation code for historical experiments; they are not the deployed external input path. The worker's calibration/presentation operations position and validate owned Chrome; they do not manufacture neural commands. Public observer requests expose no controller mutation endpoint.

Pons is reached through the VM's ordinary permitted connection. The home connection encountered a regional restriction; it was not bypassed. The VM showed normal Explore and coin detail pages. Only approved HTTPS documents and read-only resources are allowed; wallet operations, form submissions, POST requests, downloads and WebSockets to the external application are blocked. The station never navigates to its own observer site, including recovery destinations.

## Evidence and failures

### One complete native action and publication

At repaired runtime source [deb371a](https://github.com/SpecimenArchive/Specimen-Archive/tree/deb371af9b264543b5a46f61bbd69eb849faadab), command `exhibit_1789226732799_b24c502c:c000` used a native sensory exposure at **15:25:37.607 UTC** on Pons Explore. Input PNG SHA-256 was `850299d7c470a1c55c91b39b3f5d57415ea73f0db82adc0ecf3102acc12ab185`. Lower-band texture encoded L=0.15, R=0.65. Model steps 28,800–29,400 produced M=0.5779699023203891 and D=0.0179707734974196, selecting +48 px.

The proposal was recorded at 15:25:39.586, accepted/dispatched at 15:25:39.597, acknowledged at 15:25:39.956 and recaptured at 15:25:40.031 UTC. One trusted wheel event was observed at the fixed (512,465) viewport cursor; the measured scroll offset changed from 0 to 48 with no URL change. This is verified scrolling, not neural navigation. [Complete receipt, readout and publication proof](results/external-publication.json).

That episode completed 48 decisions and replayed all 2,880 sampled neural states exactly. Its immutable [Specimen Recorder publication](https://github.com/SpecimenArchive/Specimen-Archive/commit/a8a24997d629fcfdbb849d4e51533f5d2a07ffbc) was read back from GitHub and matched record SHA-256 `d70b7a246a048858340f954e2491b7fbc65806fe5518a44337d57579488eed2c`. The native video is 199.881 seconds at 1280 × 800; its bytes and metadata were checked against the published manifest. [Native recording check](results/native-video-verification.json).

Decision 15 retrieved the saved research-boundary evidence and changed the supervisor retry budget from 3 to 1, retaining the neural wheel command. The receipt acknowledged a boundary with zero displacement. [Actual memory use](results/memory-used.json). The matched held-out evaluation still found no improvement; that negative result is not overridden by showing one policy change.

The complete published episode measured **2.356 native captures/second**, median interval 260 ms and p95 1,060 ms. Median input-capture-to-dispatch time was 2,047 ms; median decision interval was 3,490 ms, with page transitions contributing longer gaps. It contained 41 visible moves, five waits and two boundaries. [Timestamp-derived performance](results/external-performance.json). These are observed values from one episode, not a 4 fps guarantee.

Five actual VM-held-out input PNGs were captured at 13:22–13:23 UTC before memory training. At clean a80d508, three matched page orders produced 15/15 intact wheel proposals, 0/15 with photoreceptor outputs disconnected and 0/15 with all motors clamped. All 45 decisions and 2,700 retained neural samples replayed exactly. This is pixel-to-proposal causal evidence; actual browser dispatch is established separately in native live records. [Machine-readable ablation](results/external-ablation.json).

The asynchronous page/native pair at b31fa11 could diverge on changing content. Run `exhibit_1789220365662_b4191d16` preserved that failure after 20 completed decisions. Its complete neural windows replayed, but the capture mismatch was not accepted as valid input. ab7e015 changed the sensory path to one native exposure; 5f5c0fc corrected a separate UTF-8 presentation mistake. Subsequent native records reproduced 48 decisions / 2,880 sampled states exactly.

The first public stream test closed with code 1013 because large high-frequency packets exceeded the observer buffer. 666771e bounded complete observer packets, reduced their maximum cadence and skipped updates under backpressure; raw recording retained all samples. The repeated 601.309-second public test delivered 2,185 identical packets to both observers with zero disconnects and 1,444 distinct native captures. Capture intervals had median 262 ms and p95 990 ms: target 4 fps is not a guarantee. One wheel attempt (`exhibit_1789222537799_956718fd:c046`) had no trusted acknowledgement and caused recovery; it remains a failed attempt.

The first forced worker interruption exposed an error-status packet lost to throttling. a0f8396 always sends state changes. At that fixed commit, the repeated test verified an exact specimen freeze, honest error/connecting status and real capture recovery under the same backend session. [Recovery evidence](results/external-recovery.json). Native timing, live UI checks, memory results and the final publication receipt are linked from [the deployment report](PUBLIC_DEPLOYMENT.md).

The selected circuit's anatomy is published; its rate dynamics, effective signs, contrast encoder and browser mapping are engineering assumptions. Causal dependence in this implementation does not establish biological browser competence or consciousness.
