# Specimen Archive — public showcase, live journal and purposeful exploration

Implement this as the next phase of the existing project. The domain is already https://specimenarchive.com/. Integrate with work already underway. This is an implementation request: inspect, make the changes, review the running result and deploy. Do not stop after summarising the brief or repeatedly ask for aesthetic approval.

The current site exposes substantial engineering as tiny text, wide diagnostic tables and expanded configuration blocks. Visitors should quickly understand what Specimen 01 does, watch an interesting encounter unfold and inspect the implementation when they choose. Build a polished public exhibit with a clear visual hierarchy and deeper pages.

## 1. Establish the actual starting point

Inspect the local checkout, deployed revision and public repository before editing. Confirm which memory, journal, research planning and control features actually run. The recent report says the entire stack runs on the VM, memory survives process restarts, recordings work and a 602-second two-observer check passed. It also reports no improvement in the learning comparison and a remaining manual Windows console login after a VM reboot. Verify these against the artifacts; preserve the working behaviour and document the limitations accurately.

Check whether a real semantic narrator is configured. The screenshots show retrieved links and raw configuration, which do not deliver the requested live journal. Finish that integration if incomplete. Use the existing provider or ask only for the missing provider/private credential configuration; never fabricate live commentary to hide an unavailable service. Continue the rest of this work while waiting.

## 2. Rebuild the public information hierarchy

Use a consistent header with the Specimen Archive identity, concise navigation and visible links to:

- X: https://x.com/SpecimenArchive
- GitHub: https://github.com/SpecimenArchive/Specimen-Archive

Remove **Methods & worksheet** from the top-right header. Replace the current public-facing document dump with designed pages sharing the site's typography, navigation and visual language:

| Destination | Visitor experience |
| --- | --- |
| Observe | The shared live session, current encounter and journal. |
| Specimens | Specimen 01 profile and a distinct Specimen 02 — Coming soon card. |
| Memory | Browsable encounters, recalled memories, sourced notes and links showing where each was used. |
| Inside the system | A visual explanation of capture, sensory encoding, circuit activity, decoding, execution, memory and narration, with actual code links. |
| Experiments | Selected recordings, reproducible runs, control comparisons and measured results. |

Use existing routes where appropriate; every destination must be a finished page with a useful direct URL. Put detailed Methods, assumptions and the worksheet within Inside the system and the footer. Keep the distinction between generated apparatus, computed neural activity and real Windows execution easy to find without repeating dense caveats throughout the homepage.

The internal review page at `/docs/previews/external-review.html` should no longer be part of the public experience. Preserve its recordings and verification evidence, present selected recordings cleanly in Experiments, remove public review links and exclude the QA page from the production build or place it behind existing private access. Merely removing a link or adding noindex does not make a page private. Verify the review page is no longer anonymously available without breaking public evidence links or deleting the underlying records. Present useful deployment information on the designed system page; do not expose raw operational files as the main visitor journey.

## 3. Give the homepage a deliberate visual composition

Retain the charcoal, warm ivory and restrained teal laboratory identity. Improve scale, spacing and readability rather than adding decoration to every panel.

- Constrain the main desktop content to approximately 1440–1520 px, centred with comfortable gutters. Explanatory paragraphs should stay around 60–75 characters per line.
- Use roughly 16–18 px body text, 14–16 px journal text and at least 12 px secondary labels. Use monospace selectively for time, telemetry and the journal. Give headings a clear scale and controls comfortable hit areas.
- Start with **Specimen 01**, its species and one short explanation. Suggested direction: “A digital larval circuit exploring the web. Follow the browser, its recorded activity and the memories collected along the way.” Adapt to verified capabilities.
- Keep the main live stage visible without an Expand requirement, but do not let the apparatus photograph consume the entire page. On wide screens, compose the apparatus, readable Windows browser capture and Live Journal together; approximately 43/32/25 percent is a starting point, not a rigid requirement. Aim for a balanced 400–480 px stage height. At narrower widths, use two media panels followed by the journal; do not crush three columns into unreadable slivers.
- Immediately below, present the specimen response and neural instruments as compact connected views. Preserve the four-view relationship: apparatus, specimen, signals and browser all describe the same session.
- Include a small set of useful live figures: verified actions, distinct pages encountered, stored memories, memories used and capture freshness. Define the counting scope. Do not duplicate large rows of tiny counters or confuse attempts with successful actions.
- Add a short current-encounter caption: the actual page/topic, why this destination was chosen, time spent and the latest verified result.
- Replace full-width raw source text, hashes, configuration strings and long archives on the homepage with readable summaries and **Explore memory**, **Inside the system** and **View experiments** buttons.

Use purposeful motion: real neural activity, a recall highlight, a new journal entry and a verified browser action. Motion should help connect events. Keep the camera composition steady, preserve natural microscope lettering and avoid ornamental glitches, fake film noise, floating particles or invented liveness. Support reduced motion and readable mobile layouts.

## 4. Make the Live Journal the human-readable centre of the experience

Build the requested terminal-style feed as a designed product component, not a raw console dump. It should translate the running system into concise observations, memory references, interpretations and next research questions.

Use event types such as **OBSERVE**, **RECALL**, **CONNECT**, **QUESTION**, **NEXT**, **ACT** and **RESULT**, with readable timestamps and restrained colour. The structure is: what was encountered → relevant memory → supported connection or open question → proposed next step → verified outcome. Do not force every encounter into every category.

Generate entries from structured evidence: current page/source content, event and frame IDs, actual retrieved memory IDs, observed outcomes, model state and accepted queue items. Link entries to those records through an Inspect control. Provide a short explanation that this is generated system narration; it explains recorded software events, rather than measuring biological thoughts.

Keep observations distinct from source-backed interpretations, hypotheses and learned changes. First-person narration is acceptable if it stays grounded and concise. A newly stored note is a memory; it does not automatically demonstrate improved performance. Show a research proposal as proposed until the supervisor accepts it and an action as complete only when execution is verified.

Give the feed personality through specific encounters and useful connections. Avoid repetitive “I am scrolling” messages, generic declarations of curiosity and invented confidence scores. Collapse repeated wheel commands into one readable activity entry while retaining the individual evidence. Add follow/pause, history and event filters; reading an older entry must not stop the VM. Do not fake a terminal typing animation that delays or reorders events.

Run narration asynchronously with bounded requests and retention. If it is unavailable, display factual structured events and the real status. Preserve journal and memory links across restarts. This remains an observation interface, not an unsolicited public chatbot or remote command console.

## 5. Make the browsing session worth watching

End the repetitive behaviour of scrolling down the same Pons listing indefinitely. Preserve external browsing and keep the observation website out of the controlled browser's destinations.

Build short, purposeful encounter sequences across approved public resources. Examples include opening a token detail, inspecting visible metadata, consulting a source about a term encountered there, visiting larval research and recalling a related earlier encounter. These are research/navigation tasks, not claims that the neural circuit understands finance. Keep activity read-only: no wallet connection, trades, signing, posting or account changes.

Use recent history, novelty and actual open questions to propose destinations. Record whether the supervisor, memory layer or semantic assistant selected the page. Neural scrolling remains attributed to the circuit only where the execution trace supports it. If adding clicks or tab navigation, implement and validate the supported control path and identify its owner; do not use scripted navigation and relabel it as a neuronal decision.

Detect boundaries, repeated unchanged views, failed navigation and excessive dwell. Advance or recover through the recorded supervisor policy instead of accumulating hundreds of identical attempts. Allow enough dwell time for visitors to follow each encounter. Keep the visible page, captured sensory input, journal topic and execution target aligned; background-tab activity must not masquerade as the visible session.

Let the journal explain a proposed research transition and then reflect its actual outcome. Add a compact journey of recent pages and a “Why this next?” link to the relevant memory/question. Provide several visibly different real encounters during a review session, while retaining the true distribution of waits, failures and recoveries. Do not retune or bypass the model solely to manufacture constant motion.

## 6. Turn the technical work into an inspectable story

Inside the system should open with an approachable interactive explanation. Selecting a real recorded action should highlight its captured input, sensory values, circuit response, decoder output and executed result. Explain what memory and narration add alongside that path. Label the selected runtime circuit separately from the full source dataset and derive counts from versioned data.

Memory should feel like an archive of experiences: useful thumbnails, clear titles, short notes and filters by topic/date/recall. Selecting an entry reveals the original encounter, subsequent retrieval and its actual use. Hide raw matching strings and hashes behind technical inspection, not inside default copy. Do not turn a URL match into a claim of visual understanding.

Experiments should present a small number of strong examples with playable recordings, concise objectives, measured outcomes, limitations, trace downloads and source revisions. Keep completed successes, controls and failures accessible. The reported learning comparison found no improvement: present it accurately as an experiment and explain what was tested. Do not relabel it “getting smarter.” Future positive claims require new measurements.

The Specimens page should give Specimen 01 a proper profile: identity, neural map source, implemented circuit, capabilities, live status and links to its memory and experiments. Add **Specimen 02 — Coming soon** as an elegant inactive next entry. Do not invent its species, capabilities, launch date, data or live status.

## 7. Make GitHub a strong companion to the website

Audit local, deployed and public revisions. Ensure the repository contains the actual implemented controller, encoder/decoder, Windows capture/control integration, persistent memory, retrieval, narrator, supervisor, adaptation experiment, recorder, publisher and relevant validation code. Map each public feature to its real files and evidence; identify anything absent instead of documenting imagined modules.

Rewrite the README with a concise explanation, public website and X links, a good current screenshot, a compact architecture diagram, verified capabilities and a usable reproduction path. Add focused documentation for the control trace, memory/journal architecture, experiments and deployment. Include configuration examples without secrets. Keep recordings in the appropriate artifact store and compact evidence in the existing evidence workflow.

Make commits at genuine implementation milestones with specific messages describing the actual changes. Preserve existing authorship, timestamps and history. Do not backdate, invent earlier development or create empty commits for appearance. Inspect pending changes and avoid committing unrelated work. Use normal reviewed pushes to the existing branches, preserving branch protections and the dedicated recorder's credential scope. Link the website to actual code revisions and evidence commits.

This brief authorises publishing the completed project changes and documentation to the existing public repository and deploying the updated existing website. Do not expand infrastructure, credential permissions or remove history as part of that authorisation.

## 8. Complete and verify the public result

Review the actual rendered site at desktop and mobile sizes, not just the build output. Check readable type, line length, composition, direct page URLs, navigation, X/GitHub links, media playback and every loading/empty/stale/error state. Review a recording of real journal entries, memory recall and varied external encounters. Avoid running a full repeated validation suite just for cosmetic adjustments; target remaining risks and satisfy existing required gates.

Verify shared session continuity with two observers, persistent records, fresh capture, public read-only access and operation after the admin RDP window closes. Do not confuse this with a cold VM restart: the reported manual console-login limitation remains documented unless separately fixed and verified. Preserve the successful deployment setup and keep DNS changes out of scope unless a concrete need arises.

Deploy the finished pages, verify public URLs and confirm the internal review page is not anonymously served. Finish with the live link, concise changes, actual GitHub commits, representative evidence and any remaining blocker. Prioritise a coherent finished experience over adding more diagnostic panels. Make reasonable design decisions autonomously and continue until the work is implemented, verified and published.
